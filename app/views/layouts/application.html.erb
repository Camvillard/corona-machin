<!DOCTYPE html>
<html>
  <head>
    <title>Corona</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <%= stylesheet_link_tag 'application', media: 'all',
    'data-turbolinks-track': 'reload' %> <%= javascript_pack_tag 'application',
    'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if @breadcrumbs %>
      <%= render 'shared/breadcrumb.html.erb', breadcrumbs: @breadcrumbs %>
    <% end %>

    <%= yield %>

    <%= render "shared/footer.html.erb" %>
  </body>
</html>
